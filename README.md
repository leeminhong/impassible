# 인파서블 (Impassible)

**2026 관광데이터 활용 공모전 · ②-2 웹·앱 구현 부문 · 지정과제 2 (오버투어리즘 분산) · 팀 MM**

> 인파(人波)를 피하는 것이 가능하다(Possible).
> 한국관광공사 관광지 집중률 예측·연관 관광지 데이터로 과밀을 예측하고, 한적한 대안 코스를 추천하는 오버투어리즘 분산 웹 서비스.

## 서비스

- 웹: https://leeminhong.github.io/impassible/
- 로그인 불필요 · 모바일/데스크톱 반응형 (900px 이상 2단 레이아웃)

## 화면 구성 (해시태그)

| 화면 | 해시태그 | 기능 |
|---|---|---|
| 붐빔 예보 | #혼잡도분석 | 지역·날짜 선택 → 요약 스트립 + 쏠림 앵커별 집중률 신호등 + 14일 스파크라인, 예보 전용 관광지 접힘 목록 |
| 대안 추천 (장소) | #과밀지역우회 #숨은명소 #광역분산 | "A 대신 B" 카드 — 연관 방문지 중 이 날 한산한 곳 + 이동 시 집중률 변화(−p) |
| 대안 추천 (날짜) | #날짜분산 | 30일 히트맵 — 같은 장소의 가장 한산한 날(★) 제안 |
| 코스 지도 | #코스추천 | 대안 앵커 + 주변 카페·볼거리 도보 동선, 붐빔 앵커→대안 이동선, 발견 카드(텍스트 복사) |
| 데이터 근거 | — | 활용 API 4종·판정 규칙·지역 30일 분산 효과 지표 |

## 활용 OpenAPI 4종 (한국관광공사)

1. 관광지 집중률 방문자 추이 예측 정보 — 향후 30일 집중률 (장소별 상대지수, 최번기 = 100)
2. 기초지자체 중심 관광지 정보 — 쏠림 앵커 정의(중심순위 50위 이내), 좌표
3. 관광지별 연관 관광지 정보 — 티맵 내비 동선 기반 함께 방문한 관광지 → 대안 후보
4. 국문 관광정보 서비스 (TourAPI) — 대안지 주변 카페·볼거리 위치기반 조회, 좌표 보강

API 키는 Vercel 서버리스 프록시(`impossible-tour-ai.vercel.app/api/kto`)에 은닉되며, 프록시가 응답을 6~24시간 캐시해 일일 트래픽 한도를 보호합니다.

## 판정 규칙

- 혼잡 3단계: 한산 <50 · 보통 50~74 · 붐빔 ≥75 (이곳 기준 상대지수 — 장소 간 비교 금지)
- 쏠림 앵커: 중심 관광지(관광 카테고리) ∩ 집중률 관광지(이름 정규화·별칭 연결) 중 중심순위 50위 이내
- 장소 대안: 앵커의 연관 관광지 중 관광 카테고리 · 앵커보다 25p 이상 낮음 · 이 날 한산 우선(없으면 보통을 "덜 붐비는 곳"으로) · 연관 20위 이내·같은 중분류 우선. 타 시군구 후보는 그 시군구의 집중률을 추가 로드해 판정. 혼잡 데이터가 없는 곳은 추천하지 않음
- 날짜 대안: 같은 장소 30일 중 집중률 최저일

## 서비스 지역

서울 9개 구(종로·중·용산·성동·마포·서초·강남·송파·강동) + 경주·강릉·속초·제주시

## 화면 설계 (v3)

- 지도가 무대: 풀블리드 OpenStreetMap 위에 데스크톱은 플로팅 패널, 모바일은 드래그 바텀시트(3단 스냅)
- 카드 ↔ 마커 브러싱: 카드에 올리면 지도 라벨이 커지고, 라벨을 누르면 카드로 스크롤
- 마커는 라벨 핀(장소명 + 이곳 기준 지수), 대안은 앵커→대안 점선으로 "분산"을 그대로 보여줌
- 사진은 TourAPI 검색 결과를 조용히 늦게 채움(없으면 자리표시)
- 색은 파인 계열 종이색 + 강조색 1개, 혼잡 3단계만 의미색. 서체는 IBM Plex Sans KR + 워드마크 고운바탕. 아이콘은 인라인 SVG
- 모션은 세 가지만: 시트/패널 이동, 라벨 팝, 요약 숫자 카운트업. `prefers-reduced-motion` 존중
- 코스 정류장마다 카카오맵 길찾기 링크, 공유는 Web Share API(미지원 시 복사)

## 구조

- `index.html` — 단일 파일 SPA (Leaflet + OpenStreetMap 타일). 빌드 없음.
- URL 파라미터: `#<signguCd>` 또는 `?r=` 지역, `?d=YYYYMMDD` 날짜, `?v=0|1|2` 화면 (캡처·공유용)
- 프록시 코드는 별도 저장소(`Impossible_tour_route`)의 `api/kto.js`.
